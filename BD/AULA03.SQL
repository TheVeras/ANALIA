-- 01
SELECT LOCATION_ID, CITY, STATE_PROVINCE
FROM HR.LOCATIONS;

-- 02
SELECT DEPARTMENT_ID, DEPARTMENT_NAME, MANAGER_ID
FROM HR.DEPARTMENTS;

-- 03
SELECT COUNTRY_ID, COUNTRY_NAME
FROM HR.COUNTRIES;

-- 04
SELECT JOB_TITLE, MAX_SALARY
FROM HR.JOBS;

-- 05
SELECT REGION_NAME 
FROM REGIONS;

-- 06
SELECT EMPLOYEE_ID, FIRST_NAME, LAST_NAME, SALARY
FROM HR.EMPLOYEES;

-- 07
SELECT EMPLOYEE_ID, START_DATE, END_DATE, JOB_ID, DEPARTMENT_ID
FROM HR.JOB_HISTORY;

/*EXERCICIOS DE FIXAÇÃO*/
-- 01
SELECT 
    L.LOCATION_ID "ID", L.STREET_ADDRESS "ENDERECO", 
    L.POSTAL_CODE "CODIGO POSTAL", L.CITY "CIDADE",
    L.STATE_PROVINCE "ESTADO", L.COUNTRY_ID "ID DO PAIS"
FROM HR.LOCATIONS L;

-- 02
SELECT * FROM HR.DEPARTMENTS;

SELECT
    D.DEPARTMENT_ID "ID DO DEPARTAMENTO",
    D.DEPARTMENT_NAME "DEPARTAMENTO",
    D.MANAGER_ID "ID DO GERENTE",
    D.LOCATION_ID "ID DA LOCALIZACAO"
FROM HR.DEPARTMENTS D;


